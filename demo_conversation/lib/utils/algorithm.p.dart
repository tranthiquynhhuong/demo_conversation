part of utils;

// int binarySearch(List list, int l, int r, int x) {
//   if (r >= l) {
//     int middle = (l + (r - l) / 2).toInt();

//     //If the element is present at middle
//     if (list[middle] == x) {
//       return middle;
//     }

//     //If the element is smaller than middle
//     if (list[middle] > x) {
//       return binarySearch(list, l, middle - 1, x);
//     }

//     return binarySearch(list, middle + 1, r, x);
//   }
//   return -1;
// }
